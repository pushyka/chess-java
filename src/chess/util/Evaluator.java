package chess.util;

import chess.components.BoardSquare;
import chess.components.FormedMove;

/**
 * Created by Gareth on 02-Oct-15.
 */
public class Evaluator {

    public FormedMove formatMove(String input)
    {
        FormedMove formedMove = new FormedMove();
        return new FormedMove();

    }

    public boolean evaluateMove(FormedMove move, BoardSquare[][] board, char player)
    {
        return false;
    }

    private int indexOf(Object e)
    {
        // return index of element in a na array
        return 0;
    }
}
